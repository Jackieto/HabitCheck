import 'package:flutter/material.dart';

class NewHabitBox extends StatelessWidget {
  const NewHabitBox({super.key});

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      content: TextField(),
    );
  }
}